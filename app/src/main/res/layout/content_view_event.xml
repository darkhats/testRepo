<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_view_event"
    tools:context=".view_event">

    <TableLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/view_event_table"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@color/lightgray"
        android:padding="5dip"
        android:stretchColumns="1,2">

        <!-- Title -->
        <TableRow
            android:id="@+id/view_event_title_row"
            android:layout_width="fill_parent"
            android:layout_height="50dip"
            android:padding="0dip"
            android:background="@color/lightgray">
            <TextView
                android:id="@+id/view_event_title_label"
                android:text="{Title}"/>
        </TableRow>

        <!-- Start Date -->
        <TableRow
            android:id="@+id/view_event_date_row"
            android:layout_width="fill_parent"
            android:layout_height="50dip"
            android:padding="0dip"
            android:background="@color/lightgray">
            <TextView
                android:id="@+id/view_event_date_label"
                android:text="From {startdate} {starttime}" />
        </TableRow>

        <!-- Start Time -->
        <TableRow
            android:id="@+id/view_event_time_row"
            android:layout_width="fill_parent"
            android:layout_height="50dip"
            android:padding="0dip"
            android:background="@color/lightgray">
            <TextView
                android:id="@+id/view_event_time_label"
                android:text="To {enddate} {endtime}" />
        </TableRow>

        <!-- Location -->
        <TableRow
            android:id="@+id/view_event_location_row"
            android:layout_width="fill_parent"
            android:layout_height="50dip"
            android:padding="0dip"
            android:background="@color/lightgray">
            <TextView
                android:id="@+id/view_event_location_label"
                android:text="at {location}"
                />
        </TableRow>

        <!-- Description -->
        <TableRow
            android:id="@+id/view_event_description_row"
            android:layout_width="fill_parent"
            android:layout_height="50dip"
            android:padding="0dip"
            android:background="@color/lightgray">
            <TextView
                android:id="@+id/view_event_description_label"
                android:text="{description}" />
        </TableRow>

        <!-- Cancel Button -->
        <Button
            android:id="@+id/backToCalendar"
            android:text="Back"
            android:layout_width="fill_parent"
            android:layout_height="60dip"
            android:background="@color/darkgray"
            android:layout_marginLeft="2dip"
            android:layout_marginRight="2dip"
            android:layout_marginTop="5dip">
        </Button>

    </TableLayout>

</RelativeLayout>
